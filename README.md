This is an example to code a program to control linked list with C++. The function included:
- Add node (begin, end, anywhere);
- Delete node (begin, end, anywhere);
- Sort node (Bubble sort).
- Find node.
